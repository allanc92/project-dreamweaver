{
  "name": "project-dreamweaver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:server": "node server/index.js",
    "test": "node --test tests/**/*.test.js",
    "check": "node --check server/index.js && node --check shared/storyGenerator.js"
  }
}
